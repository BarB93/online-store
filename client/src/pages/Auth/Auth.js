import React from 'react'

import styles from './Auth.module.scss'

const Auth = () => {
    return (
        <div className={'container ' + styles.container}> 
            AUTH PAGES
        </div>
    ) 
    
}

export default Auth